<nav class="navbar navbar-expand-lg navbar-dark bg-primary">

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContentSearch" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <img class="searchIconeMobile" src="{{ asset('build/images/Vector.svg') }}" alt="searchIcone">
  </button>

  <div class="flex">
    <img class="logo" src="{{ asset('build/images/Logo.svg') }}" alt="logo">
    <a class="navbar-brand" href="{{ path('app_home_page')}}">
      SerinityLab</a>
  </div>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <img class="searchIconeMobile" src="{{ asset('build/images/burger.svg') }}" alt="searchIcone">
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContentSearch">
    <ul class="navbar-nav" id="navbarSupportedContentSearch">
      <li class="nav-item">
        <form class="form-inline my-2 my-lg-0">
          {{ form_start(searchForm) }}
          {{ form_widget(searchForm.query, {'attr': {'class': 'form-control navRadius search'}}) }}
          <button class="btn" type="submit"><img class="searchIcone" src="{{ asset('build/images/Vector.svg') }}" alt="searchIcone"></button>
          {{ form_end(searchForm) }}
        </form>
      </li>
    </ul>
  </div>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_activity')}}">Activités</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_contact')}}">Contact</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Qui sommes-nous ?</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_login')}}">Connexion</a>
      </li>
    </ul>
  </div>
  <div class="userNav">
    <img class="bodyMassage mx-2" src="{{ asset('build/images/bodyMassage.svg') }}" alt="bodyMassage">
    <a href="{{ path('app_activity')}}"><img class="ticketLogo mx-2" src="{{ asset('build/images/fluent_ticket-diagonal-16-regular.svg') }}" alt="ticketLogo"></a>
    <img class="userLogo" src="{{ asset('build/images/mingcute_user-1-line.svg') }}" alt="userLogo">
  </div>
</nav>

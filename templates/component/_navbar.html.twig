<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <img class="logo" src="{{ asset('build/images/Logo.svg') }}" alt="logo">
  <a class="navbar-brand" href="{{ path('app_landing_page')}}">
    SerinityLab</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">

      <li class="nav-item">
        <form class="form-inline my-2 my-lg-0">
            {{ form_start(form) }}
            {{ form_widget(form.query, {'attr': {'class': 'form-control navRadius search'}}) }}
            <button class="btn" type="submit"><img class="searchIcone" src="{{ asset('build/images/Vector.svg') }}" alt="searchIcone"></button>
            {{ form_end(form) }}
        </form>
      </li>
    </ul>
  </div>
  <div class="userNav">
    <img class="ticketLogo " src="{{ asset('build/images/fluent_ticket-diagonal-16-regular.svg') }}" alt="ticketLogo">
    <img class="userLogo" src="{{ asset('build/images/mingcute_user-1-line.svg') }}" alt="userLogo">
  </div>
</nav>
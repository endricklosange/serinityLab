{% extends 'adminBase.html.twig' %}

{% block title %}Company{% endblock %}

{% block body %}
    <h1>Company</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company.id }}</td>
            </tr>
            <tr>
                <th>CompanyName</th>
                <td>{{ company.companyName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_company_index') }}">back to list</a>

    <a href="{{ path('app_admin_company_edit', {'id': company.id}) }}">edit</a>

    {{ include('admin/company/_delete_form.html.twig') }}
{% endblock %}
